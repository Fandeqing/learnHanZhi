# Local development: use Railway's public TCP proxy / public database URL.
# Do not use postgres.railway.internal locally; it only works inside Railway.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/railway"
